{
  "name": "{{name}}",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "sync-engine check && sync-engine artifacts check --config generated.config.ts && tsc --noEmit",
    "generate": "sync-engine artifacts pin",
    "typecheck": "tsc --noEmit",
    "principle": "bun src/concepts/noting/noting.test.ts",
    "start": "bun src/scenario.ts"
  },
  "dependencies": {
    "@mit-sdg/sync-engine": "{{version}}"
  },
  "devDependencies": {
    "typescript": "^5.9.0"
  }
}
