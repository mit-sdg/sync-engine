{
  "name": "{{name}}",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "sync-engine check --config generated.config.ts && sync-engine artifacts check --config generated.config.ts && tsc --noEmit",
    "generate": "sync-engine artifacts pin",
    "typecheck": "tsc --noEmit",
    "principle": "bun src/concepts/noting/noting.test.ts",
    "start": "bun src/scenario.ts"
  },
  "dependencies": {
    "@mit-sdg/sync-engine": "{{version}}"
  },
  "devDependencies": {
    "typescript": "{{typescript}}"
  },
  "engines": {
    "bun": "{{bun}}",
    "node": "{{node}}"
  },
  "packageManager": "{{packageManager}}"
}
