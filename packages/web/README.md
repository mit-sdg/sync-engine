# @mit-sdg/sync-engine-web

`@mit-sdg/sync-engine-web/realization` realizes a named rendered interface as Fetch claims.
Rendering forms checked HTML fragments; this package owns the opening document
and browser transport. It does not open a listener.
