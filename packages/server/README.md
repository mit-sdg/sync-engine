# @mit-sdg/sync-engine-server

`@mit-sdg/sync-engine-server/serve` checks Fetch claims together, opens one listener,
and executes the Serving concept around admitted interactions. Withdrawal stops
new admissions and waits for response bodies or streams the service still owns.
It does not drain or shut down target systems.
